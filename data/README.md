# Data layout

- `derived/` contains the tracked project CSV outputs retained for reproducibility.
- `raw/` is intentionally reserved for authorized Big Data Cup source exports and is not included in the repository.

Do not commit data that cannot be redistributed under the competition or provider terms.
